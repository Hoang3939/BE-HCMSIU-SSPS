{
  "compilerOptions": {
    /* File Layout - Bắt buộc mở khóa để phân tách code nguồn và code build */
    "rootDir": "./src",
    "outDir": "./dist",

    /* Environment Settings */
    "module": "NodeNext",          /* Hỗ trợ ES Modules hiện đại nhất cho Node.js */
    "moduleResolution": "NodeNext",
    "target": "ESNext",            /* Sử dụng các tính năng JS mới nhất */
    "lib": ["ESNext"],
    "types": ["node"],             /* Để nhận diện các hàm của Node.js */

    /* Output */
    "sourceMap": true,
    "declaration": true,
    "declarationMap": true,

    /* Interop - Cần thiết để import các thư viện CommonJS như express, cors */
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,

    /* Stricter Typechecking */
    "strict": true,
    "noUncheckedIndexedAccess": true,

    /* Style Options */
    "skipLibCheck": true,
    
    /* Tắt hoặc chỉnh lại để không bị bắt lỗi "import type"*/
    "verbatimModuleSyntax": false, 
    "isolatedModules": true,
    "moduleDetection": "force"
  },
  "include": ["src/**/*"],          /* Chỉ định TypeScript chỉ quét trong thư mục src */
  "exclude": ["node_modules"]
}